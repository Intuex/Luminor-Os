# Luminor-Os

**The download instructions are for arch linux if you dont have an arch linux machine consider an vm**

Download Tools:
```bash
chmod +x download_tools.sh #this may require your machines password
./download_tools.sh
```

Compile and run:
```bash
chmod +x run.sh
./run.sh
```
